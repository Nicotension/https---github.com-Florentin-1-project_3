<nav class="navbar">
	<div class="navbar-container">
		<a href="{{ path ('app_homepage')}}" class="nav-logo">Home</a>
		<div class="nav-items">

			{% if is_granted('ROLE_ADMIN')%}
				<div class="nav-item dropdown">
					<button class="dropdown-button">Products</button>
					<div class="dropdown-menu">
						<a href="{{ path ('app_product_index')}}">Show all</a>
						<a href"{{ path ('app_catalog')}}">Product 1</a>
						<a href="#">Product 2</a>
						<a href="#">Product 3</a>
					</div>

				</div>
			{% endif %}

			{% if is_granted('ROLE_USER')%}
				<div class="nav-item dropdown">
					<button class="dropdown-button">Products</button>
					<div class="dropdown-menu">
						<a href="{{ path ('app_user_index')}}">Show all</a>
						<a href="#">Product 1</a>
						<a href="#">Product 2</a>
						<a href="#">Product 3</a>
					</div>

				</div>

			{% endif %}

			{% if is_granted("IS_AUTHENTICATED_FULLY")!= true %}

				<a href="/register" class="nav-item">Register</a>
				<a href="/login" class="nav-item">Login</a>
			{% endif %}


			<a href="{{ path ('app_orders_index')}}" class="nav-item cart">Cart</a>
			<form class="search-form" action="#" method="get">
				<input type="text" placeholder="Search..." class="search-input">
				<button type="submit" class="search-button">Search</button>

				{% if is_granted("IS_AUTHENTICATED_FULLY")%}

					<div class="nav-item dropdown">
						<button class="dropdown-button">My Profile</button>
						<div class="dropdown-menu">
							<a href="{{ path('app_user_show') }}" type=" submit" class=" search-button">View Profile</a>
							<a href="#">My Orders</a>

						</div>

					</div>


					<a href="/logout" type="submit" class="search-button">Logout</a>
				{% endif %}


			</form>
		</div>
	</div>
</nav>
<div class="oline"></div>
