{% extends 'base.html.twig' %}

{% block title %}Questions{% endblock %}

{% block body %}
<style>
h1 {
    font-size: 2.5rem;
    font-weight: bold;
    color: rgb(232, 184, 36);
    margin-top: 20px;
}
</style>

<div class=" pageTitle m-2 text-center">
    <h1>Questions</h1>
</div>
    <a class="m-5" href="{{ path('app_questions_index') }}">&#9664;&#9664; back to list</a>


<div class="container mt-5">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ question.question }}</td>
            </tr>
        </tbody>
    </table>

    {% if is_granted('ROLE_ADMIN') %}

    {# <a class="btn btn-warning" href="{{ path('app_questions_edit', {'id': question.id}) }}">edit</a> #}

    {{ include('questions/_delete_form.html.twig') }}
     {% endif %}

    </div>
{% endblock %}
